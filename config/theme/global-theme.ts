import { StyleSheet } from "react-native";

export const theme = StyleSheet.create({
  globalMargin: {
    marginHorizontal: 20
  },
  rightFab: {
    position: 'absolute',
    bottom: 20,
    right: 20
  }
})